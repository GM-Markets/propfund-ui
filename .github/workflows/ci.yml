name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6
        with:
          version: 11.16.0

      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint

      - name: Type-check
        run: pnpm typecheck

      - name: Unit tests
        run: pnpm test

      - name: Build
        run: pnpm build
        env:
          # Build-time placeholders so `next build` doesn't fail on missing env.
          HSC_API_BASE_URL: http://localhost:8000
          HSC_CLIENT_ID: ci_placeholder
          HSC_CLIENT_SECRET: ci_placeholder
          SESSION_COOKIE_SECRET: ci_placeholder_session_secret_min_32_bytes
          NEXT_PUBLIC_SITE_URL: http://localhost:3000
          NEXT_PUBLIC_HSC_API_BASE_URL: http://localhost:8000

  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: pnpm/action-setup@v6
        with:
          version: 11.16.0

      - uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - run: pnpm install --frozen-lockfile

      - name: Install Playwright browsers
        run: pnpm exec playwright install --with-deps chromium

      - name: Run e2e tests
        run: pnpm test:e2e
        env:
          HSC_API_BASE_URL: http://localhost:8000
          HSC_CLIENT_ID: ci_placeholder
          HSC_CLIENT_SECRET: ci_placeholder
          SESSION_COOKIE_SECRET: ci_placeholder_session_secret_min_32_bytes
          NEXT_PUBLIC_HSC_API_BASE_URL: http://localhost:8000
